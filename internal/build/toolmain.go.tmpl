// Generated by relay build. Do not edit; edit the manifest and SKILL.md
// instead and rebuild.
//
// This binary is the generic Relay runtime plus this tool's embedded assets.
// It carries both the ability and the knowledge of how to use it (spec §63).
package main

import (
	_ "embed"
	"os"

	"relay/pkg/toolruntime"
)

// The manifest is machine truth: what this tool can do (spec §6).
//
//go:embed manifest.yaml
var manifestYAML []byte

// The skill is LLM guidance: how an agent should use it (spec §7).
//
//go:embed SKILL.md
var skillMD []byte

func main() {
	os.Exit(toolruntime.Main(manifestYAML, skillMD))
}
